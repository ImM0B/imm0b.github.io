node_modules
docs/.vitepress/cache
docs/.vitepress/dist

# docs/src and docs/.vitepress/navigation.json are generated by `npm run import`
# but stay committed: CI has no access to the Obsidian vault.

# Leftover from an earlier attempt
.sidebar-data.json
